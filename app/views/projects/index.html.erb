<h1>All Projects</h1>
  <h2>Active Projects</h2>
  <% @pivotal_tracker_projects.each do |project|  %>
    <div>
      <%= project.name %>
      <%= project.id %>
      <%= link_to 'Show', project_path(project) %>
    </div>
  <% end %>
